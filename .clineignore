# ===================================================================
# Cline Ignore Rules for Sydney Rental Hub Project
# Purpose: Focus on the modern Vue 3 frontend development.
# ===================================================================

# 1. Ignore universal build artifacts, dependencies, and logs
node_modules/
dist/
build/
__pycache__/
*.log
*.pyc
.DS_Store

# Ignore environment files, but keep the example for reference
.env*
!.env.example

# 2. Ignore legacy, archived, and irrelevant modules
# These are not part of the current development scope.
archive/

crawler/


# 3. Ignore specific sub-directories within active modules
# We need the source code for context, but not the dependencies.
apps/web/node_modules/
apps/web/dist/

mcp-server/node_modules/
mcp-server/build/

backend/.venv/
backend/venv/

database/sydney-suburbs-name.json

# ===================================================================
# By default, all other files are INCLUDED.
# This ensures that memory-bank/, apps/web/, backend/ source,
# and root configuration files are always accessible.
# ===================================================================
