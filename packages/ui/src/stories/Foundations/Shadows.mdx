import { Meta } from '@storybook/blocks';

<Meta title="设计规范 (Foundations)/Shadows (阴影)" />

# Shadows (阴影)

阴影系统为界面元素提供了深度和层级感。

export const ShadowBlock = ({ shadow, value }) => (
  <div style={{ display: 'flex', alignItems: 'center', marginBottom: '20px' }}>
    <div style={{ width: '150px', fontFamily: 'monospace' }}>{shadow}</div>
    <div style={{ height: '80px', width: '120px', boxShadow: `var(${shadow})`, border: '1px solid var(--color-border-default)', borderRadius: 'var(--radius-md)' }} />
  </div>
);

## 阴影令牌

<ShadowBlock shadow="--shadow-sm" />
<ShadowBlock shadow="--shadow-md" />
<ShadowBlock shadow="--shadow-lg" />
<ShadowBlock shadow="--shadow-xl" />
