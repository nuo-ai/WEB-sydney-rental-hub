# ==============================================================================
# Sydney Rental Hub - Monorepo Environment Variables
#
# Instructions:
# 1. Copy this file to `.env` in the root directory.
# 2. Fill in the required values for the services you intend to run.
#
# Prerequisites:
# This repository uses pnpm as the primary package manager.
# Ensure pnpm is installed before running any services.
# ==============================================================================

# =====================================================
# Part 1: AI Model & Service API Keys
# Required for `apps/mcp-server` and other AI-related functionalities.
# =====================================================
ANTHROPIC_API_KEY="your_anthropic_api_key_here"       # Required: Format: sk-ant-api03-...
PERPLEXITY_API_KEY="your_perplexity_api_key_here"     # Optional: Format: pplx-...
OPENAI_API_KEY="your_openai_api_key_here"             # Optional, for OpenAI/OpenRouter models. Format: sk-proj-...
GOOGLE_API_KEY="your_google_api_key_here"             # Optional, for Google Gemini models.
MISTRAL_API_KEY="your_mistral_key_here"               # Optional, for Mistral AI models.
XAI_API_KEY="YOUR_XAI_KEY_HERE"                       # Optional, for xAI AI models.
AZURE_OPENAI_API_KEY="your_azure_key_here"            # Optional, for Azure OpenAI models (requires endpoint in .taskmaster/config.json).
OLLAMA_API_KEY="your_ollama_api_key_here"             # Optional: For remote Ollama servers that require authentication.
GITHUB_API_KEY="your_github_api_key_here"             # Optional: For GitHub import/export features. Format: ghp_... or github_pat_...

# =====================================================
# Part 2: Backend Configuration (`apps/backend`)
# Required for running the Python FastAPI server.
# =====================================================
# Runtime environment (development, staging, production)
ENVIRONMENT=development

# Mandatory: Full PostgreSQL connection string.
# Used by FastAPI/SQLAlchemy to connect to the database.
DATABASE_URL="postgres://user:pass@host:5432/dbname"

# Connection pool sizes (effective when not in DB_NO_POOL mode)
# For development: keep small to avoid exhausting pgBouncer session limits
DB_POOL_MIN_SIZE=1
DB_POOL_MAX_SIZE=3

# Bypass psycopg2 connection pool entirely (use direct connections).
# Set to true in development if you see:
# "FATAL: MaxClientsInSessionMode: max clients reached"
DB_NO_POOL=false

# =====================================================
# Part 3: Frontend Configuration (`apps/web`)
# Vite-specific variables. Must be prefixed with `VITE_`.
# =====================================================
# The public URL of the backend API that the frontend will communicate with.
VITE_PUBLIC_API_BASE="http://127.0.0.1:8000"

# Optional: For integrating with services like Google Maps.
# VITE_GOOGLE_MAPS_API_KEY="your_google_maps_api_key_here"
