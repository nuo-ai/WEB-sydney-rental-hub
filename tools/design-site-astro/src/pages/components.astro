---
/**
 * Route wrapper: /components
 * Fix: render the folder index instead of redirect (avoid dev redirect loops)
 */
import ComponentsIndex from "./components/index.astro";
export const prerender = true;
---

<ComponentsIndex />
