---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="SRH Design Site · 首页" showBackbar={false}>
  <Fragment slot="breadcrumb">
    <span>首页</span>
  </Fragment>

  <style>
    .grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 16px;
    }
    @media (max-width: 720px) { .grid { grid-template-columns: 1fr; } }

    .card {
      border: 1px solid var(--component-button-secondary-border, #e5e7eb);
      border-radius: var(--component-button-radius, 8px);
      background: var(--component-button-secondary-bg, #fff);
      color: var(--component-button-secondary-text, #222);
      padding: 16px;
      text-decoration: none;
      display: block;
      transition: var(--component-button-transition, all 0.2s ease-in-out);
    }
    .card:hover {
      background: var(--component-button-secondary-hover-bg, #f3f4f6);
      color: var(--component-button-secondary-hover-text, #111);
      border-color: var(--component-button-secondary-hover-border, #d1d5db);
    }
    .card h2 { margin: 0 0 6px 0; font-size: 16px; }
    .card p { margin: 0; opacity: 0.8; line-height: 1.6; }
    .section { margin-top: 24px; }
    .section h1 { margin: 0 0 12px 0; font-size: 20px; }
    .desc { margin: 0 0 16px 0; color: rgba(0,0,0,0.6); }
    html[data-theme='dark'] .desc { color: rgba(255,255,255,0.7); }
  </style>

  <section class="section">
    <h1>SRH Design Site</h1>
    <p class="desc">设计 Token 与组件演示站。可切换顶部“暗色”按钮验证主题。</p>
    <div class="grid">
      <a class="card" href="/tokens/">
        <h2>Tokens</h2>
        <p>Design Tokens 索引与演示（如 Black & White 色板、间距、阴影等）。</p>
      </a>
      <a class="card" href="/components/">
        <h2>Components</h2>
        <p>组件演示索引（如 Button：primary/secondary/ghost/link × sm/md/lg）。</p>
      </a>
    </div>
  </section>
</BaseLayout>
