---
/**
 * Route wrapper: /tokens
 * Fix: render the actual index page instead of redirect
 * This avoids redirect loops when accessing /tokens/ during dev.
 */
import TokensIndex from "./tokens/index.astro";
export const prerender = true;
---

<TokensIndex />
