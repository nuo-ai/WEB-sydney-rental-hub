[pytest]
# 注册自定义标记，消除 PytestUnknownMarkWarning（与 pytest-timeout 插件配合可启用真实超时）
markers =
    timeout: mark test with a timeout (requires pytest-timeout plugin)

# 可选：限制默认测试路径，避免误扫大目录（按需开启）
# testpaths = tests

# 可选：常用参数（按需开启）
# addopts = -q -ra
