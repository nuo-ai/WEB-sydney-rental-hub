# 悉尼租房平台 (Sydney Rental Platform)

这是一个专为悉尼学生设计的租房平台，以“到校通勤时间”为核心，帮助用户快速找到最合适的房源。

---

## 快速开始 (本地开发)

**1. 环境配置**
   - 复制根目录下的 `.env.example` 文件，并重命名为 `.env`。
   - 根据你的环境（特别是Supabase数据库连接信息）填写 `.env` 文件中的必要变量。

**2. 安装依赖**
   - **后端 (Python)**: `pip install -r requirements.txt`
   - **MCP Server (Node.js)**: `npm install --prefix mcp-server`

**3. 一键启动所有服务**
   - 运行 `python scripts/start_services.py` 来启动所有本地服务。
   - 在终端中按 `Ctrl+C` 可以一次性关闭所有服务。

---

## 📚 项目文档

本项目的所有详细文档，包括项目结构、部署指南、自动化工作流等，都集中在我们的文档中心。

**[➡️ 点击这里进入文档中心](./docs/index.md)**

---

*该项目由AI软件工程师Cline协助开发和维护。*
